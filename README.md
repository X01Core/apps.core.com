# apps.core.com
# Core Application
This repository contains the core application for the apps.core.com platform. It serves as the backbone for various services and functionalities provided by the platform.

git remote remove origin // Remove existing origin if any
git remote add origin git@github-x01core:X01Core/apps.core.com.git
git push origin main